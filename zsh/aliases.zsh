alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command
alias zshconfig="code ~/.zshrc"
alias hostsconfig="code /etc/hosts"
alias ohmyzshconfig="code ~/.oh-my-zsh"
alias brewgradate="brew update && brew upgrade"
alias k="kubectl"
alias fw="cd ~/footway"
alias zet="cd ~/zetterstrom"
alias gpyi="git pull && yarn install"
alias gpni="git pull && npm install"
alias gpyiyd="git pull && yarn install && yarn dev"
alias gpnind="git pull && npm install && npm run dev"
